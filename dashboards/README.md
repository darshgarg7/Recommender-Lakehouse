# Dashboard contracts

Production dashboards should cover pipeline health, throughput/cost, and ML quality. Source them
from the `monitoring` schema and include manifest failures, quarantine rate, duplicate rate,
checkpoint age, feature/embedding drift, cohort metrics, exposure, and recommendation domain mix.
